---
title: Another Post
date: 2025-10-20
---

# Another Post

This is another post I'm writing to see if my new build script is working. I think it is, but let's find out.

## Handling Code

Does my build script handle code snippets? Actually no, it doesn't - that should all be handled by `marked`, but let's see how this all turns out just for fun.

```javascript
let x = 1;
let y = 2;
let z = x + y;
console.log(z);
```
