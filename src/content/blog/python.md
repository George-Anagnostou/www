---
title: Writing Python
date: 2025-10-21
---

# Writing Python

What is it like to write `python` in `markdown`? I assume it's pretty easy, but let's see what it looks like!

```python
import pandas as pd

df = pd.read_csv("my_data.csv")
df["sum"] = df["prices"].sum()

df
```

How does this work?

What about other languages?

```C++
#include <iostream>

int main() {
  std::cout << "Hello, World!" << std::endl;
  return 0;
}
```
